#TO find the sum of odd elements
import math
NUMBERS =numbers.append(int(input("enter n elements of the list numbers")))

def= sum_of_odd_elements(NUMBERS):
    SUM=0
    for i in range(0,NUMBERS-1):
        if i % 2  ==0:
            sum += NUMBERS[i]
        return sum
    if SUM<0:
        print("server-1 deallocated "math.abs(SUM))
    else:
        print("server-1 allocated SUM units of memory")