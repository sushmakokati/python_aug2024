import numpy as np
array1 = np.arange(1, 10) # The range is from 1 to 9. Note that the 2nd Arg is excluded.
array2 = np.arange(1,20,3)# The 3rd Arg depicts the increment for every element in the List

print(array1)
print(array2)
